<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FENATS · Admin · Importación</title>
  <style>
    :root{
      --bg:#0b1220;
      --card:rgba(255,255,255,.08);
      --border:rgba(255,255,255,.12);
      --text:rgba(255,255,255,.92);
      --muted:rgba(255,255,255,.70);
      --shadow:0 20px 60px rgba(0,0,0,.35);
      --ok:#22c55e; --warn:#f59e0b; --bad:#ef4444; --blue:#3b82f6;
      --btn: rgba(255,255,255,.10);
      --btnBorder: rgba(255,255,255,.16);
    }
    *{box-sizing:border-box}
    body{
      margin:0; min-height:100vh; color:var(--text);
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
      background:
        radial-gradient(800px 500px at 20% 10%, rgba(34,197,94,.18), transparent 60%),
        radial-gradient(900px 600px at 90% 30%, rgba(59,130,246,.20), transparent 60%),
        radial-gradient(700px 500px at 40% 90%, rgba(245,158,11,.16), transparent 60%),
        var(--bg);
      padding: 24px;
    }
    .wrap{width:min(980px,100%); margin:0 auto}
    .top{
      display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap;
      margin-bottom:14px;
    }
    .brand{display:flex;align-items:center;gap:10px;font-weight:900;letter-spacing:.2px}
    .dot{width:12px;height:12px;border-radius:999px;background:var(--blue);box-shadow:0 0 0 6px rgba(255,255,255,.06)}
    .nav{display:flex;gap:10px;flex-wrap:wrap}
    .btn{
      display:inline-flex;align-items:center;justify-content:center;
      padding:10px 12px;border-radius:14px;border:1px solid var(--btnBorder);
      background:var(--btn);color:var(--text);text-decoration:none;font-weight:900;font-size:14px;
      cursor:pointer;
    }
    .btnPrimary{background:rgba(255,255,255,.14);border-color:rgba(255,255,255,.22)}
    .card{
      background:var(--card);
      border:1px solid var(--border);
      border-radius:18px;
      box-shadow:var(--shadow);
      overflow:hidden;
    }
    .head{padding:18px 22px 14px;border-bottom:1px solid var(--border)}
    h1{margin:0;font-size:20px}
    p{margin:8px 0 0;color:var(--muted);font-size:13px;line-height:1.45}
    .body{padding:18px 22px 20px}
    .grid{display:grid;grid-template-columns: 1fr 1fr; gap:12px}
    .field{
      background:rgba(255,255,255,.05);
      border:1px solid rgba(255,255,255,.10);
      border-radius:14px;
      padding:12px;
    }
    .label{font-size:12px;color:var(--muted);margin-bottom:6px}
    .input{
      width:100%;
      border-radius:12px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.06);
      padding:10px 10px;
      color:var(--text);
      outline:none;
      font-size:14px;
    }
    .hint{margin-top:8px;color:var(--muted);font-size:12px;line-height:1.4}
    .bar{
      margin-top:12px;
      display:flex;gap:10px;flex-wrap:wrap;align-items:center;
    }
    .alert{
      margin-top:12px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.06);
      padding:12px;
      border-radius:14px;
      font-size:13px;
      line-height:1.45;
    }
    .okBox{border-color: rgba(34,197,94,.35); background: rgba(34,197,94,.10)}
    .warnBox{border-color: rgba(245,158,11,.35); background: rgba(245,158,11,.10)}
    @media (max-width: 820px){
      .grid{grid-template-columns:1fr}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="top">
      <div class="brand"><span class="dot"></span><span>FENATS · Importación</span></div>
      <div class="nav">
        <a class="btn" href="/admin">Dashboard</a>
        <a class="btn" href="/admin/members">Socios</a>
        <a class="btn" href="/admin/logout">Salir</a>
      </div>
    </div>

    <div class="card">
      <div class="head">
        <h1>Importar planilla de socios</h1>
        <p>
          Sube un archivo Excel. El sistema detecta la tabla aunque no esté desde la primera celda.
          Se crean socios nuevos o se actualizan existentes por <b>RUT</b>.
        </p>
      </div>
      <div class="body">
        <form method="post" action="/admin/upload" enctype="multipart/form-data">
          <div class="grid">
            <div class="field">
              <div class="label">Filial (affiliate)</div>
              <input class="input" name="affiliate" value="FENATS OCTAVA" />
              <div class="hint">Ej: FENATS OCTAVA / FENATS ARAUCO / etc.</div>
            </div>
            <div class="field">
              <div class="label">Fuente / referencia de importación</div>
              <input class="input" name="importSource" value="Arauco 12/2025" />
              <div class="hint">Ej: “Arauco 12/2025”, “Convenio Parque 01/2026”, etc.</div>
            </div>
          </div>

          <div style="height:12px"></div>

          <div class="field">
            <div class="label">Archivo Excel</div>
            <input class="input" type="file" name="file" accept=".xlsx,.xls" required />
            <div class="hint">Formatos soportados: .xlsx y .xls</div>
          </div>

          <div class="bar">
            <button class="btn btnPrimary" type="submit">Importar</button>
            <a class="btn" href="/admin/members">Ver listado</a>
          </div>
        </form>

        <% if (message) { %>
          <div class="alert <%= (stats && stats.skipped && stats.skipped > 0) ? 'warnBox' : 'okBox' %>">
            <div><b><%= message %></b></div>
            <% if (stats) { %>
              <div style="margin-top:8px">
                Filas detectadas: <b><%= stats.totalRows %></b> ·
                Creados: <b><%= stats.created %></b> ·
                Actualizados: <b><%= stats.updated %></b>
                <% if (typeof stats.skipped !== "undefined") { %>
                  · Omitidos: <b><%= stats.skipped %></b>
                <% } %>
              </div>
            <% } %>
          </div>
        <% } %>

      </div>
    </div>
  </div>
</body>
</html>

