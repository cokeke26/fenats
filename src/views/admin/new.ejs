<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FENATS · Admin · Nuevo socio</title>
  <style>
    :root{
      --bg:#0b1220;
      --card:rgba(255,255,255,.08);
      --border:rgba(255,255,255,.12);
      --text:rgba(255,255,255,.92);
      --muted:rgba(255,255,255,.70);
      --shadow:0 20px 60px rgba(0,0,0,.35);
      --ok:#22c55e;
      --warn:#f59e0b;
      --bad:#ef4444;
      --blue:#3b82f6;
      --btn: rgba(255,255,255,.10);
      --btnBorder: rgba(255,255,255,.16);
    }
    *{box-sizing:border-box}
    body{
      margin:0; min-height:100vh; color:var(--text);
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
      background:
        radial-gradient(800px 500px at 20% 10%, rgba(34,197,94,.18), transparent 60%),
        radial-gradient(900px 600px at 90% 30%, rgba(59,130,246,.20), transparent 60%),
        radial-gradient(700px 500px at 40% 90%, rgba(245,158,11,.16), transparent 60%),
        var(--bg);
      padding: 24px;

      /* ✅ para footer abajo */
      display:flex;
      flex-direction:column;
    }

    /* ✅ empuja el footer al fondo cuando hay poco contenido */
    .page{ flex:1; }

    .wrap{width:min(980px,100%); margin:0 auto}
    .top{
      display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap;
      margin-bottom:14px;
    }
    .brand{display:flex;align-items:center;gap:10px;font-weight:900;letter-spacing:.2px}
    .dot{width:12px;height:12px;border-radius:999px;background:var(--blue);box-shadow:0 0 0 6px rgba(255,255,255,.06)}
    .nav{display:flex;gap:10px;flex-wrap:wrap}
    .btn{
      display:inline-flex;align-items:center;justify-content:center;
      padding:10px 12px;border-radius:14px;border:1px solid var(--btnBorder);
      background:var(--btn);color:var(--text);text-decoration:none;font-weight:900;font-size:14px;
      cursor:pointer;
    }
    .btnPrimary{background:rgba(255,255,255,.14);border-color:rgba(255,255,255,.22)}
    .card{
      background:var(--card);
      border:1px solid var(--border);
      border-radius:18px;
      box-shadow:var(--shadow);
      overflow:hidden;
    }
    .head{padding:18px 22px 14px;border-bottom:1px solid var(--border)}
    h1{margin:0;font-size:20px}
    p{margin:8px 0 0;color:var(--muted);font-size:13px;line-height:1.45}
    .body{padding:18px 22px 20px}
    .grid{display:grid;grid-template-columns: 1fr 1fr; gap:12px}
    .field{
      background:rgba(255,255,255,.05);
      border:1px solid rgba(255,255,255,.10);
      border-radius:14px;
      padding:12px;
    }
    .label{font-size:12px;color:var(--muted);margin-bottom:6px}
    .input, .select{
      width:100%;
      border-radius:12px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.06);
      padding:10px 10px;
      color:var(--text);
      outline:none;
      font-size:14px;
    }
    .bar{margin-top:14px;display:flex;gap:10px;flex-wrap:wrap}
    .alert{
      margin-top:14px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.06);
      padding:12px;
      border-radius:14px;
      font-size:13px;
      line-height:1.45;
    }
    .okBox{border-color: rgba(34,197,94,.35); background: rgba(34,197,94,.10)}
    .badBox{border-color: rgba(239,68,68,.35); background: rgba(239,68,68,.10)}
    @media (max-width: 820px){
      .grid{grid-template-columns:1fr}
    }

    /* --- Fix selects en Windows/Chrome --- */
    select.input{
      appearance: none;
      -webkit-appearance: none;
      -moz-appearance: none;
      background-color: rgba(255,255,255,.06);
      color: var(--text);
    }
    select.input option{
      background-color: #0b1220;
      color: rgba(255,255,255,.92);
    }
    select.input:focus,
    input.input:focus{
      outline: none;
      border-color: rgba(255,255,255,.28);
      box-shadow: 0 0 0 4px rgba(255,255,255,.06);
    }
  </style>
</head>
<body>

  <div class="page">
    <div class="wrap">
      <div class="top">
        <div class="brand">
          <span class="dot"></span>
          <span>FENATS · Nuevo socio</span>
        </div>
        <div class="nav">
          <a class="btn" href="/admin">Panel</a>
          <a class="btn" href="/admin/members">Socios</a>
          <a class="btn" href="/admin/upload">Importar Excel</a>
          <a class="btn" href="/admin/logout">Salir</a>
        </div>
      </div>

      <div class="card">
        <div class="head">
          <h1>Ingreso manual de socio</h1>
          <p>
            Crea o actualiza un socio por <b>RUT</b>.
            Si el RUT ya existe, se actualizan sus datos sin cambiar el token (a menos que lo renueves).
          </p>
        </div>

        <div class="body">
          <form method="post" action="/admin/new">
            <div class="grid">
              <div class="field">
                <div class="label">Nombre completo</div>
                <input
                  class="input"
                  name="fullName"
                  placeholder="Ej: Juan Pérez Soto"
                  value="<%= values?.fullName ?? '' %>"
                  required
                />
              </div>

              <div class="field">
                <div class="label">RUT</div>
                <input
                  class="input"
                  name="rut"
                  placeholder="Ej: 9.313.137-1"
                  value="<%= values?.rut ?? '' %>"
                  required
                />
              </div>

              <div class="field">
                <div class="label">Filial (opcional)</div>
                <input
                  class="input"
                  name="affiliate"
                  placeholder="Ej: FENATS OCTAVA"
                  value="<%= values?.affiliate ?? '' %>"
                />
              </div>

              <div class="field">
                <div class="label">Estado</div>
                <select class="input" name="status">
                  <option value="ACTIVE" <%= (values?.status ?? 'ACTIVE') === 'ACTIVE' ? 'selected' : '' %>>Vigente</option>
                  <option value="INACTIVE" <%= values?.status === 'INACTIVE' ? 'selected' : '' %>>No vigente</option>
                </select>
              </div>

              <div class="field">
                <div class="label">Género (opcional)</div>
                <select class="input" name="gender">
                  <option value="" <%= !values?.gender ? 'selected' : '' %>>No especificar</option>
                  <option value="MALE" <%= values?.gender === 'MALE' ? 'selected' : '' %>>Masculino</option>
                  <option value="FEMALE" <%= values?.gender === 'FEMALE' ? 'selected' : '' %>>Femenino</option>
                  <option value="OTHER" <%= values?.gender === 'OTHER' ? 'selected' : '' %>>Otro</option>
                </select>
              </div>
            </div>

            <div class="bar">
              <button class="btn btnPrimary" type="submit">Guardar socio</button>
              <a class="btn" href="/admin/members">Volver al listado</a>
            </div>
          </form>

          <% if (message) { %>
            <div class="alert okBox"><b><%= message %></b></div>
          <% } %>

          <% if (error) { %>
            <div class="alert badBox"><b><%= error %></b></div>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <%- include("../partials/footer") %>
</body>
</html>

