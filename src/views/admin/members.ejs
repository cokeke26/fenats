<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FENATS · Admin · Socios</title>
  <style>
    :root{
      --bg:#0b1220;
      --card:rgba(255,255,255,.08);
      --border:rgba(255,255,255,.12);
      --text:rgba(255,255,255,.92);
      --muted:rgba(255,255,255,.70);
      --shadow:0 20px 60px rgba(0,0,0,.35);
      --ok:#22c55e; --warn:#f59e0b; --bad:#ef4444; --blue:#3b82f6;
      --btn: rgba(255,255,255,.10);
      --btnBorder: rgba(255,255,255,.16);
    }
    *{box-sizing:border-box}
    body{
      margin:0; min-height:100vh; color:var(--text);
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
      background:
        radial-gradient(800px 500px at 20% 10%, rgba(34,197,94,.18), transparent 60%),
        radial-gradient(900px 600px at 90% 30%, rgba(59,130,246,.20), transparent 60%),
        radial-gradient(700px 500px at 40% 90%, rgba(245,158,11,.16), transparent 60%),
        var(--bg);
      padding: 24px;
    }
    .wrap{width:min(1150px,100%); margin:0 auto}
    .top{
      display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap;
      margin-bottom:14px;
    }
    .brand{display:flex;align-items:center;gap:10px;font-weight:900;letter-spacing:.2px}
    .dot{width:12px;height:12px;border-radius:999px;background:var(--blue);box-shadow:0 0 0 6px rgba(255,255,255,.06)}
    .nav{display:flex;gap:10px;flex-wrap:wrap}
    .btn{
      display:inline-flex;align-items:center;justify-content:center;
      padding:10px 12px;border-radius:14px;border:1px solid var(--btnBorder);
      background:var(--btn);color:var(--text);text-decoration:none;font-weight:900;font-size:14px;
      cursor:pointer;
    }
    .btnPrimary{background:rgba(255,255,255,.14);border-color:rgba(255,255,255,.22)}
    .card{
      background:var(--card);
      border:1px solid var(--border);
      border-radius:18px;
      box-shadow:var(--shadow);
      overflow:hidden;
    }
    .head{padding:18px 22px 14px;border-bottom:1px solid var(--border)}
    h1{margin:0;font-size:20px}
    p{margin:8px 0 0;color:var(--muted);font-size:13px;line-height:1.45}
    .body{padding:16px 22px 20px}
    .search{
      display:flex; gap:10px; flex-wrap:wrap; align-items:center;
      margin-bottom: 12px;
    }
    .input{
      flex:1;
      min-width: 240px;
      border-radius:14px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.06);
      padding:10px 12px;
      color:var(--text);
      outline:none;
      font-size:14px;
    }
    .hint{color:var(--muted);font-size:12px;margin-top:6px;line-height:1.4}

    /* ✅ Mobile scroll wrapper */
    .tableScroll{
      overflow-x:auto;
      -webkit-overflow-scrolling: touch;
      padding-bottom: 6px;
    }
    /* ✅ Force table to be wider so scroll appears */
    .tableMin{
      min-width: 980px;
    }

    table{width:100%;border-collapse:separate;border-spacing:0 10px}
    th{
      text-align:left;
      color:rgba(255,255,255,.70);
      font-size:12px;
      font-weight:900;
      padding: 0 10px;
      white-space: nowrap;
    }
    td{
      padding: 12px 10px;
      background: rgba(255,255,255,.05);
      border-top: 1px solid rgba(255,255,255,.10);
      border-bottom: 1px solid rgba(255,255,255,.10);
      font-size: 14px;
      vertical-align: middle;
    }
    td:first-child{
      border-left: 1px solid rgba(255,255,255,.10);
      border-top-left-radius: 14px;
      border-bottom-left-radius: 14px;
    }
    td:last-child{
      border-right: 1px solid rgba(255,255,255,.10);
      border-top-right-radius: 14px;
      border-bottom-right-radius: 14px;
    }
    .muted{color:var(--muted)}
    .badge{
      display:inline-flex;align-items:center;gap:8px;
      padding:8px 10px;border-radius:999px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.06);
      font-size:12px;font-weight:950;
      white-space:nowrap;
    }
    .bDot{width:8px;height:8px;border-radius:999px}
    .ok{background:var(--ok)} .warn{background:var(--warn)}
    .actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}
    .mini{
      padding:9px 10px;
      border-radius:12px;
      font-size:12px;
      width:auto;
      white-space: nowrap;
    }
    .link{color:rgba(255,255,255,.92)}
    code{color:rgba(255,255,255,.88); white-space: nowrap;}

    /* ✅ Ya NO escondemos columnas: ahora se hace scroll horizontal */
    @media (max-width: 980px){
      .wrap{width:min(980px,100%)}
    }
  </style>
</head>
<body>
  <% const canEdit = role === "ADMIN" || role === "SUPERADMIN"; %>

  <div class="wrap">
    <div class="top">
      <div class="brand"><span class="dot"></span><span>FENATS · Socios</span></div>
      <div class="nav">
        <a class="btn" href="/admin">Panel</a>

        <% if (canEdit) { %>
          <a class="btn btnPrimary" href="/admin/new">Agregar socio</a>
          <a class="btn" href="/admin/upload">Importar Excel</a>
        <% } %>

        <a class="btn" href="/admin/logout">Salir</a>
      </div>
    </div>

    <div class="card">
      <div class="head">
        <h1>Gestión de socios</h1>
        <p>
          Busca por RUT, nombre o token.
          <% if (canEdit) { %>
            Desde aquí puedes activar/inactivar, regenerar token y descargar QR.
          <% } else { %>
            Acceso de solo lectura: puedes ver socios y descargar QR.
          <% } %>
        </p>
      </div>
      <div class="body">
        <form class="search" method="get" action="/admin/members">
          <input class="input" name="q" value="<%= q %>" placeholder="Ej: 9.313.137-1, Juan Pérez, o token..." />
          <button class="btn btnPrimary" type="submit">Buscar</button>
          <a class="btn" href="/admin/members">Limpiar</a>
        </form>
        <div class="hint">
          Consejo: Puedes ingresar un RUT con o sin puntos. El sistema lo detecta.
        </div>

        <div style="height:12px"></div>

        <!-- ✅ Wrapper para scroll horizontal en móvil -->
        <div class="tableScroll">
          <table class="tableMin">
            <thead>
              <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>RUT</th>
                <th>Filial</th>
                <th>Estado</th>
                <th>Links</th>
                <th>Token</th>
                <th style="text-align:right">Acciones</th>
              </tr>
            </thead>
            <tbody>
              <% members.forEach(m => { %>
                <tr>
                  <td class="muted"><%= m.id %></td>

                  <td>
                    <div style="font-weight:900"><%= m.fullName %></div>
                    <div class="muted" style="font-size:12px">
                      Actualizado: <%= new Date(m.updatedAt).toLocaleString("es-CL") %>
                    </div>
                  </td>

                  <td>
                    <div style="font-weight:900"><%= m.rutMasked %></div>
                    <div class="muted" style="font-size:12px">Normalizado: <%= m.rut %></div>
                  </td>

                  <td class="muted"><%= m.affiliate || "-" %></td>

                  <td>
                    <% if (m.status === "ACTIVE") { %>
                      <span class="badge"><span class="bDot ok"></span>VIGENTE</span>
                    <% } else { %>
                      <span class="badge"><span class="bDot warn"></span>NO VIGENTE</span>
                    <% } %>
                  </td>

                  <td>
                    <div><a class="link" href="/s/<%= m.token %>" target="_blank" rel="noreferrer">Validación</a></div>
                    <div><a class="link" href="/credencial/<%= m.token %>" target="_blank" rel="noreferrer">Credencial</a></div>
                  </td>

                  <td><code><%= m.token %></code></td>

                  <td>
                    <div class="actions">
                      <a class="btn mini" href="/qr/<%= m.token %>.png" target="_blank" rel="noreferrer">Descargar QR</a>

                      <% if (canEdit) { %>
                        <form method="post" action="/admin/members/<%= m.id %>/toggle" style="display:inline">
                          <button class="btn mini" type="submit"><%= m.status === "ACTIVE" ? "Inactivar" : "Activar" %></button>
                        </form>

                        <form method="post" action="/admin/members/<%= m.id %>/regen-token" style="display:inline">
                          <button class="btn mini" type="submit">Renovar token</button>
                        </form>
                      <% } %>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>

        <div class="hint" style="margin-top:8px">
          En móvil: desliza horizontalmente para ver todas las columnas.
        </div>

      </div>
    </div>
  </div>
</body>
</html>




